# Temperature Analysis

This repository contains a Python program that analyzes weekly temperature data using **for loops**, **continue**, and **break** statements, without using built-in functions like `max()` or `min()`.

1. Finding maximum and minimum temperatures
2. Counting hot days
3. Implementing an alert system for extreme temperatures

